<!doctype html>
<html lang="en-us">
  <head>
    <meta charset="utf-8">
    <title>Bugs reopened in {{ channel }} {{ major }}</title>
  </head>
  <body>
    <p>Hi there,</p>
    <p>Here are the bugs reopened in {{ channel }} {{ major }}.</p>
    <ul>
      {% for bugid, link in links %}
      <li><a href="{{ link }}">{{ bugid }}</a></li>
      {% endfor %}
    </ul>
    <p>Sincerely,<br>
      Release Management Bot
    </p>
  </body>
</html>
